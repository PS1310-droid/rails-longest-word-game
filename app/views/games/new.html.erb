<h2>Longest Word Game</h2>

<div class="letters">
  <% @letters.each do |letter| %>
    <span class="letter-box"><%= letter %></span>
  <% end %>
</div>

<p class="mt-4">What is the longest word you can find?</p>

<!--
  This form will send a POST request to /score.
  The 'name' attributes ("word", "letters") will
  become the keys in our 'params' hash.
-->
<form action="/score" method="post" data-turbo="false" class="d-flex">
  <!--
    This is the security token Rails requires for all POST requests.
    It protects against Cross-Site Request Forgery (CSRF).
  -->
  <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
  <!--
    This is the *crucial* hidden field.
    It passes the @letters array (joined into a string)
    to the 'score' action so it can be checked.
  -->
  <%= hidden_field_tag :letters, @letters.join(' ') %>

  <input type="text" name="word" class="form-control" placeholder="Enter your word...">
  <input type="submit" value="Play!" class="btn btn-primary ms-2">
</form>
